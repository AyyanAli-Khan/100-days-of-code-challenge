"use strict";
function convertCase(string) {
    let upperCase = string.toUpperCase();
    let lowerCase = string.toLowerCase();
    console.log(`Uppercase : ${upperCase} , Lowercase : ${lowerCase} `);
}
convertCase("TypeScript");
