"use strict";
function strCharacters(string) {
    let length = string.length;
    return `The number of characters in given string is : ${length}`;
}
console.log(strCharacters("TypeScript is a superset of JavaScript"));
